@using SCORM1.Models.RigidCourse;
@using SCORM1.Models.ViewModel;
@model CursoRigidoViewModel
@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>FlashQuestion</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.flashTest.FlashTestName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.flashTest.FlashTestName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Enunciado)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Enunciado)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
                @Html.ActionLink("Delete", "DeleteFlashQuestionConfirm", new { FlashQuestionId = Model.FlashQuestionId}) |
        </div>
    }
</div>
